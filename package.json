{
  "name": "automaton-fxs",
  "version": "3.0.0-beta7",
  "description": "Bunch of automaton fxs",
  "repository": "https://github.com/FMS-Cat/automaton-fxs",
  "author": "FMS-Cat",
  "main": "dist/automaton-fxs.js",
  "module": "dist/automaton-fxs.module.js",
  "types": "types/index.d.ts",
  "typesVersions": {
    "<3.9": {
      "*": [
        "ts3.4/*"
      ]
    }
  },
  "license": "MIT",
  "scripts": {
    "dev": "cross-env DEV=1 ESM=1 SERVE=1 rollup -w -c -o dist/automaton-fxs.module.js",
    "all": "yarn lint && yarn test && yarn clean && yarn build",
    "clean": "rimraf dist/ ts*/ types/",
    "test": "jest",
    "lint": "eslint \"src/**/*.{ts,tsx}\"",
    "build": "yarn build-dev && yarn build-prod && yarn build-esm && yarn build-esmprod && yarn build-types",
    "build-dev": "cross-env DEV=1 rollup -c -o dist/automaton-fxs.js",
    "build-prod": "rollup -c -o dist/automaton-fxs.min.js",
    "build-esm": "cross-env DEV=1 ESM=1 rollup -c -o dist/automaton-fxs.module.js",
    "build-esmprod": "cross-env ESM=1 rollup -c -o dist/automaton-fxs.module.min.js",
    "build-types": "tsc --declaration --declarationDir ./types --emitDeclarationOnly && downlevel-dts types ts3.4/types"
  },
  "files": [
    "/dist/",
    "/ts*/",
    "/types/",
    "/LICENSE",
    "/package.json",
    "/README.md"
  ],
  "private": false,
  "devDependencies": {
    "@fms-cat/automaton": "3.0.0-beta7",
    "@rollup/plugin-replace": "^2.3.3",
    "@rollup/plugin-typescript": "^5.0.2",
    "@types/jest": "^26.0.10",
    "@typescript-eslint/eslint-plugin": "^3.10.1",
    "@typescript-eslint/parser": "^3.10.1",
    "cross-env": "^7.0.2",
    "downlevel-dts": "^0.6.0",
    "eslint": "^7.7.0",
    "eslint-plugin-jest": "^23.20.0",
    "gh-pages": "^3.1.0",
    "jest": "^26.4.2",
    "rimraf": "^3.0.2",
    "rollup": "^2.26.8",
    "rollup-plugin-banner": "^0.2.1",
    "rollup-plugin-serve": "^1.0.4",
    "rollup-plugin-terser": "^7.0.1",
    "ts-jest": "^26.3.0",
    "ts-node": "^9.0.0",
    "tslib": "^2.0.1",
    "typescript": "^4.0.2"
  }
}
