import { Colors } from '../constants/Colors';
import styled from 'styled-components';

const InspectorHr = styled.div`
  margin: 0.25rem 0;
  height: 0.125rem;
  width: 100%;
  background: ${ Colors.back3 };
`;

export { InspectorHr };
